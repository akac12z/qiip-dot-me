---
/**
 * para este formulario necesito:
 * - la url base
 * los utms:
 *  - utm_source -> plataforma de dónde viene el usuario: google, facebook, instagram, newsletter...
 *  - utm_medium -> tipo de tráfico/campaña que trae a los usuarios: cpc, email, social, banner...
 *  - utm_campaign -> nombre de la campaña que quieres hacer tracking
 *  - utm_term (opcional) -> palabra clave para la cual se está enseñando el anuncio (puedes no tener)
 *  - utm_content (opcional) -> contenido concreto en el que el usuario hace click (puedes tener uno y por ende, podría no serte útil este o tener varios en la propia página y tener que diferenciarlos con este parámetro)
 */
---

<from>
  <section>
    <label for="">Base URL</label>
    <input type="text" placeholder="https://ejemplo.com/pagina" required />
    <span class=""> The page you want to drive traffic to</span>
  </section>

  <section></section>
</from>

<!-- <form id="utmForm">
  <section class="form-group">
    <label for="baseUrl">
      Base URL <span class="required">*</span>
    </label>
    <input
      type="url"
      id="baseUrl"
      placeholder="https://ejemplo.com/pagina"
      required
    />
    <span class="help-text"> The page you want to drive traffic to</span>
  </section>

  <section class="grid">
    <section class="form-group">
      <label for="utmSource">
        utm_source <span class="required">*</span>
      </label>
      <input
        type="text"
        id="utmSource"
        placeholder="google, facebook, newsletter"
        required
      />
      <span class="help-text">Traffic source</span>
    </section>

    <section class="form-group">
      <label for="utmMedium">
        utm_medium <span class="required">*</span>
      </label>
      <input
        type="text"
        id="utmMedium"
        placeholder="cpc, email, social, banner"
        required
      />
      <span class="help-text">Marketing channel</span>
    </section>
  </section>

  <section class="form-group">
    <label for="utmCampaign">
      utm_campaign <span class="required">*</span>
    </label>
    <input
      type="text"
      id="utmCampaign"
      placeholder="verano_2024, lanzamiento_producto"
      required
    />
    <span class="help-text">Promotional campaign name</span>
  </section>

  <section class="grid">
    <section class="form-group">
      <label for="utmTerm">
        utm_term <span class="optional">(opcional)</span>
      </label>
      <input type="text" id="utmTerm" placeholder="zapatos_running, keyword" />
      <span class="help-text">Do you have paid keywords?</span>
    </section>

    <section class="form-group">
      <label for="utmContent">
        utm_content <span class="optional">(opcional)</span>
      </label>
      <input
        type="text"
        id="utmContent"
        placeholder="banner_top, link_footer, version_a"
      />
      <span class="help-text">Diferencia entre anuncios</span>
    </section>
  </section>

  <section class="button-group">
    <button type="submit" class="btn-primary">
      <span>Generate URL</span>
    </button>

    <span class="btn-secondary" id="resetBtn">Clean form</span>
  </section>
</form> -->
